using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StudentVMVMVMVM : MonoBehaviour
{
    //public string[]studentNames;

    //public List<string> studentNames = new List<string>();

    // [Range(1,10)]public int index;   

    //public string name;

   // public int width;

   // public int height;

  //  public int depth;

    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(GameObject.CreatePrimitive(PrimitiveType.Cube), new Vector3(5f, 5f, 5f), Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        // Debug.Log(studentNames[index-1]);

       // if(Input.GetKeyDown(KeyCode.Space))  
            { 
              // studentNames.Add(name);
            }
    }
}
